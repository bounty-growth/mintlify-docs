---
title: "Contributing"
description: "How to preview, validate, and safely maintain the public Bounty docs."
---

# Contributing

This repository contains public Bounty documentation. Keep implementation details, private infrastructure names, customer data, and internal runbooks out of this repo.

## Local Preview

Install the Mintlify CLI:

```bash
npm i -g mint
```

Run a local preview from the repository root:

```bash
mint dev
```

## Validate Before Publishing

```bash
mint validate
mint broken-links
```

After the docs site is public, check agent readiness:

```bash
mint score https://docs.bountygrowth.com
```

## Public Content Checklist

Before opening a pull request, check that the docs do not include:

- Private source paths or package internals.
- API route implementation details.
- Database, warehouse, or infrastructure credentials.
- Local environment file names or values.
- Internal customer names, test data, or unreleased product plans.

## Agent Skills

Mintlify serves custom skills from `.mintlify/skills/`. Each skill lives in its own folder with a `SKILL.md` file.

Do not symlink skills from private repositories into this repo. Copy only audited, public-safe skill files.
